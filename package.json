{
  "name": "server",
  "version": "1.0.0",
  "description": "server for join me app",
  "main": "app.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "join me group",
  "license": "ISC",
  "dependencies": {
    "nodemon": "^2.0.20"
  }
}
